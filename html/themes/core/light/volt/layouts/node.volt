{% block body %}

	<div class="container">
		<div class="row">
			<div class="">
				<div class="">
					<h1 class="page-title">{{ title }}</h1>
					<h2 class="page-description">{{ abstract }}</h2>
					<div class="page-body">{{ body }}</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}
