name: PHP Composer

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Phalcon Action
      uses: MilesChou/phalcon-action@0.0.1
      with:
        args: php -m
